// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("hexdump", "for hexdump support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("hexdump", "enable hexdump support", "no");

if (PHP_HEXDUMP != "no") {
	EXTENSION("hexdump", "hexdump.c");
}

